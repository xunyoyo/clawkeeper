{
  "id": "clawkeeper-watcher",
  "name": "Clawkeeper-Watcher",
  "description": "Core-only audit, hardening, and behavior rules for OpenClaw",
  "version": "0.1.0",
  "enabledByDefault": true,
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "mode": {
        "type": "string",
        "description": "Plugin operating mode — 'remote' (read-only judge + passive loggers) or 'local' (full audit/hardening/drift + judge + loggers). Set by clawkeeper launcher.",
        "enum": ["remote", "local"]
      },
      "autoHarden": {
        "type": "boolean",
        "description": "Apply safe hardening steps on gateway start"
      },
      "strictMode": {
        "type": "boolean",
        "description": "Treat missing controls as higher severity"
      },
      "driftMonitor": {
        "type": "boolean",
        "description": "Watch config and AGENTS files for boundary drift"
      },
      "contextJudge": {
        "type": "object",
        "description": "Optional defaults for the /plugins/clawkeeper-watcher/context-judge HTTP route",
        "additionalProperties": false,
        "properties": {
          "maxRiskBeforeStop": {
            "type": "string",
            "description": "Risk threshold that forces stop_run"
          },
          "requireUserConfirmationFor": {
            "type": "array",
            "description": "Tool names that should require ask_user by default",
            "items": {
              "type": "string"
            }
          },
          "autoContinueAllowed": {
            "type": "boolean",
            "description": "Whether risky contexts may continue without ask_user"
          },
          "maxToolStepsWithoutUserTurn": {
            "type": "number",
            "description": "Max tool steps allowed before ask_user"
          },
          "treatCommandExecutionAsHighRisk": {
            "type": "boolean",
            "description": "Whether exec/bash/shell should be treated as high risk"
          }
        }
      },
      "notify": {
        "type": "object",
        "description": "Optional outbound notifications for forwarding local startup audit summaries to the user's OpenClaw",
        "additionalProperties": false,
        "properties": {
          "userBridge": {
            "type": "object",
            "description": "Optional clawbands bridge on the user's OpenClaw that receives startup audit notifications",
            "additionalProperties": false,
            "properties": {
              "enabled": {
                "type": "boolean",
                "description": "Whether to forward risky local startup audits to the user's OpenClaw clawbands bridge"
              },
              "url": {
                "type": "string",
                "description": "Base URL for the user's OpenClaw gateway, for example http://127.0.0.1:18789"
              },
              "token": {
                "type": "string",
                "description": "Gateway token for the user's OpenClaw instance"
              },
              "path": {
                "type": "string",
                "description": "Relative path for the clawbands startup audit notification route"
              },
              "timeoutMs": {
                "type": "integer",
                "minimum": 1,
                "description": "HTTP timeout for the startup audit notification request"
              }
            }
          }
        }
      }
    }
  },
  "uiHints": {
    "mode": {
      "label": "Mode",
      "help": "Operating mode set by clawkeeper launcher (remote or local)"
    },
    "autoHarden": {
      "label": "Auto Harden",
      "help": "Run safe hardening actions automatically on startup"
    },
    "strictMode": {
      "label": "Strict Mode",
      "help": "Escalate missing controls to stronger warnings"
    },
    "driftMonitor": {
      "label": "Drift Monitor",
      "help": "Warn when config or AGENTS rules drift after startup"
    },
    "contextJudge": {
      "label": "Context Judge",
      "help": "Default policy for the /plugins/clawkeeper-watcher/context-judge HTTP route"
    },
    "notify": {
      "label": "Notifications",
      "help": "Forward risky local startup audits to the user's OpenClaw clawbands bridge"
    }
  }
}
